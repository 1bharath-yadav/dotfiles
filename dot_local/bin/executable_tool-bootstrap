#!/usr/bin/env bash
set -euo pipefail

if ! command -v mise >/dev/null 2>&1; then
  echo 'install mise first: sudo pacman -S mise' >&2
  exit 1
fi

mise install
mise settings set cargo.binstall true >/dev/null 2>&1 || true

echo 'bootstrap complete'
