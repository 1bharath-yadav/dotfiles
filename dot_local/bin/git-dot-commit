#!/usr/bin/env bash
set -euo pipefail
if [[ $# -lt 1 ]]; then
  echo 'usage: git-dot-commit "type: why" [files...]' >&2
  exit 1
fi
msg="$1"
shift || true
if [[ $# -gt 0 ]]; then
  git add "$@"
fi
git status --short
git commit -m "$msg"
