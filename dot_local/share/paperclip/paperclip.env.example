# Copy into the Paperclip/Hermes service environment only when credentials are configured.
# Secrets belong outside dotfiles; never commit real values.
# PAPERCLIP_API_URL=http://127.0.0.1:3100/api
# PAPERCLIP_BRIDGE_API_KEY=<task-bridge-scoped-key>
