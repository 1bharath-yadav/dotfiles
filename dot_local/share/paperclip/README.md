# Paperclip AI on Arch Linux

Paperclip is installed through its managed CLI at .
Instance data remains in .

The instance is loopback-only on  and uses embedded PostgreSQL.

Hermes has a first-party Paperclip adapter in the Paperclip distribution.
Prefer the  adapter when Paperclip should launch the local Hermes
CLI; use  when using a long-lived Hermes API server.
