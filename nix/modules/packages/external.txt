# External package managers — non-Nix tools.
#
# Primary runtime versions and CLI tools are now managed by mise
# in `nix/modules/programs/mise/default.nix`.
#
# sync-external-tools runs `mise install` to materialize them.

# ── Termux (pkg) ──────────────────────────────────────────────────────────────
pkg:termux-api
pkg:termux-tools
pkg:openssh
pkg:gnupg