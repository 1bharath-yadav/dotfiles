# Kanata configuration plan

This directory will contain:
- kanata.kbd
- user service

Planned features:
- jk -> Escape chord
- Space hold -> Navigation layer
- Home-row navigation
- Hyprkan integration

NOTE: The actual kanata.kbd has not been generated yet because it must match the installed Kanata version's exact configuration syntax.